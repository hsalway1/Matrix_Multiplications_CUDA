# Matrix_Multiplications_CUDA
This project explores multiple CUDA matrix multiplication implementations, starting with a naive kernel and progressing through optimizations such as shared-memory tiling and register tiling. New implementations and performance improvements will be added as I continue learning and experimenting with CUDA optimization techniques.
